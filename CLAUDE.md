# DailyTechFlow — 项目指令

## 项目背景

每天凌晨 3:30 自动运行的科技新闻内容流水线。抓取 3 条新闻 → 生成中英文文章 + 视频脚本 → 生成封面图 → 合成语音 → 合成视频 → 发布微信公众号。

详细设计见 `docs/architecture.md`，任务进度见 `docs/progress.md`。

## 项目结构

```
src/            6 个独立模块，每个模块只做一件事
templates/      Hyperframes HTML 模板
output/         每日产物，按 YYYY-MM-DD 归档
docs/           设计文档（不要修改，除非用户明确要求）
config.yaml     API 密钥（不要提交）
```

## 编码规范

- 纯 Python，不引入不必要的框架
- 每个模块统一签名：`run(date_str: str, config: dict) -> bool`
- 每个模块只读写 `output/YYYY-MM-DD/`，不跨目录
- 任何异常都要被 `main.py` 捕获并写入 `run.log`，不能让整个流水线崩溃

## Agent Behavior（Karpathy 原则）

**不做隐藏假设** — 有歧义先问，不要猜。比如不确定某个 API 的调用方式，问清楚再写。

**简洁优先** — 只写解决问题所需的最少代码。不加未要求的功能、抽象、配置项或"灵活性"。一个模块 80 行能解决就不写 300 行。

**精准修改** — 只改必须改的文件。不"顺手"重构无关模块。每一行修改都能追溯到当前任务。

**完成才是完成** — 每个模块写完后必须有对应单测，且在终端跑通，才算完成。不要只写代码不验证。

## 特别禁令

- 禁止在没有要求的情况下修改 `docs/` 下的任何文档
- 禁止在模块间引入共享全局状态
- 禁止为"将来可能需要"的功能写代码
- 禁止把多个模块的逻辑合并到一个文件

## Communication Style

默认用中文回复。简洁，不废话。有问题先问，不要自行假设后闷头写。
